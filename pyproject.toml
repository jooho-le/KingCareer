[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.115, <1", "uvicorn>=0.34, <1", "argon2-cffi>=23.1, <26", "networkx>=3.4, <4", "langgraph>=1.0, <2", "langgraph-checkpoint-sqlite>=3.0, <4", "httpx>=0.28, <1", "python-dotenv>=1.0, <2", "psycopg[binary]>=3.2, <4" ]
requires-python = "~=3.12.0"
